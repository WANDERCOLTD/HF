# AI-to-DB Guard Pattern

> Full guard lifecycle (invariant → guard → register → wire → pin → retire) and the
> survives-hardening classification live in [`docs/kb/guards-process.md`](../../docs/kb/guards-process.md).
> The catalogue of every existing guard is [`docs/kb/guard-registry.md`](../../docs/kb/guard-registry.md).
> This file holds the AI-to-DB *trigger* and pattern; the KB holds the procedure.

## Rule: Never let AI output directly drive entity creation

When AI returns structured data that will be used to create, update, or delete database records, **always** insert a deterministic validation step between the AI response and the DB write.

```
AI proposes → Guard validates → Code executes
```

## When This Applies

Any code path where:
1. AI returns JSON/structured output (tool calls, classification, grouping, scoring)
2. That output determines **how many** records to create, **what type** of entity, or **which parent** to attach to
3. The code iterates the AI output and calls `prisma.*.create` / `update` / `delete`

## Required Checks

| AI decides | Guard must check |
|-----------|-----------------|
| How many entities to create | Max count cap (not just in prompt — enforce in code) |
| Entity type / classification | Whitelist validation with fallback |
| Parent-child relationships | FK exists in DB before write |
| Parameter/key names | Validate against known set from DB |
| Destructive operations (delete + rebuild) | Wrap in transaction, validate new data before deleting old |
| Module / LO identity by slug / ref | Use `resolveModuleByLogicalId(curriculumId, slug)` from `lib/curriculum/resolve-module.ts` — never bare `findFirst({where:{slug}})`. Slugs are per-parent unique, not global (#407). |
| Pipeline FK writes driven by AI output | `Call.curriculumModuleId` and `CallerModuleProgress.moduleId` are written from AI-returned slugs (`learningAssessment.moduleId`). Must scope by `playbookId → curriculumId` before resolving the slug. ESLint rule `hf-curriculum/no-unscoped-slug-lookup` blocks regressions. |

## Pattern: Validate-then-write

```typescript
// BAD: AI output → direct DB write
for (const group of aiResult.groups) {
  await prisma.subject.create({ data: { name: group.name } });
}

// GOOD: AI output → validate → DB write
const { validated, fixes } = validateManifest(aiResult);
console.log(`[guard] Applied ${fixes.length} fix(es)`);
for (const group of validated.groups) {
  await prisma.subject.create({ data: { name: group.name } });
}
```

## Existing Guards

| Location | Guard | What it prevents |
|----------|-------|-----------------|
| `lib/content-trust/validate-manifest.ts` | `validateManifest()` | AI creating too many subjects, promoting pedagogy to subjects |
| Pipeline `callScore` | Numeric clamping `[0, 1]` | Score overflow |
| Pipeline `callTarget` | `validateTargets()` guardrail pass | Target value outside safe range |
| `generate-groups` | `validateGroupType()` whitelist | Invalid group type enum |
| `lib/curriculum/resolve-module.ts` | `resolveModuleByLogicalId(curriculumId, slug)` — throws when curriculumId is empty | AI-returned slugs from `learningAssessment.moduleId` resolving to a cross-playbook CurriculumModule (#407 Opal/Freya/Tessa). |
| `eslint-rules/no-unscoped-slug-lookup.mjs` | Custom ESLint rule, error severity | New `prisma.curriculumModule.find*({where:{slug,...}})` without `curriculumId`. Same for `learningObjective` + `ref` + `moduleId`. (#411) |
| `scripts/check-fk-consistency.ts` (CI step 5) | SQL queries for cross-playbook leaks + orphan-LO + dangling-soft-FK | Bad data reaching dev/staging from a slipped-through code path (#415); LO/assertion soft-FK lag after `reconcile-lo-linkage.ts` cadence drift (#615). |
| `lib/content-trust/resolve-config.ts` | `categoryToTeachMethod()` short-circuit on INSTRUCTION_CATEGORIES + `assertNoLearnerMethodOnInstructionCategory()` at extraction boundaries | Tutor-only directives ("Speak to learn", "Pedagogical Principles", etc.) being assigned a learner-facing `teachMethod` and rendered as quiz questions in learner sessions (#605). Pre-fix the function's `recall_quiz` fallback silently fired for every INSTRUCTION_CATEGORIES member. |
| `lib/knowledge/cleanup-placeholder-subjects.ts` | `unlinkNonPrimaryPlaybookSubjects(playbookId, keepSubjectId)` called from `wizard-tool-executor.create_course` after the course-scoped Subject is linked | Two creation paths (`quick-launch/analyze` writes a domain-level Subject; `wizard-tool-executor.create_course` writes a course-scoped Subject) both linking via `PlaybookSubject` with no shared knowledge of each other. DB `@@unique([playbookId, subjectId])` only blocks pair-duplicates, not cross-subject coexistence. Produced duplicate CONTENT AUTHORITY sections in the composed prompt (#607 / IELTS Prep Lab). |
| `lib/curriculum/resolve-module.ts::resolveModuleSlug` (write path, #611 Fix A) + `scripts/migrate-caller-attribute-lo-mastery-keys.ts` (drain, #614) | Canonical slug-form for the `{moduleId}` token in `CallerAttribute.key` for `lo_mastery:*` entries. `resolveModuleSlug(curriculumId, moduleIdOrName)` runs at AGGREGATE write; the drain script reclassifies legacy name-form rows to canonical via the same helper and soft-deletes the legacy row with `validUntil = NOW()`. | AI echoing the module's display title ("Part 1: Familiar Topics") into `lo_mastery:{moduleId}:{loRef}` keys. Pre-fix the dual-key shape produced non-deterministic mastery reads downstream — the same LO carried `0.9` under name-form and `0` under slug-form, picked at random by `Object.keys()` insertion order. Reader at `transforms/modules.ts:702` is intentionally tolerant during the drain grace window; tighten once `callerAttributeOldKeyFormCount` audit counter reads 0 everywhere. |
| `hooks/use-pending-changes-tray.tsx` (collector) + `app/api/recompose/apply/route.ts` (writer) + `eslint-rules/no-ai-fanout-all.mjs` (build-time guard) + #878 AI-surface push helper | Five-layer guard: (1) TypeScript `TrayEntry` interface forces `aiSuggested: boolean` on every push, (2) reducer makes `aiSuggested` sticky across re-pushes and freezes `beforeValue`, (3) zod `.strict()` validates the apply payload server-side, (4) runtime guard at `apply/route.ts:125-126` rejects `aiSuggested + toggleAll` (cohort fan-out from an AI-touched batch), (5) ESLint rule blocks AI tool executors passing `fanoutScope: 'all'` directly. Audit row written for every Save & apply regardless of toggle state. | AI tool executors writing directly to `PlaybookConfig` / `BehaviorTarget` / `AnalysisSpec` without human review; cohort fan-out triggered from an AI suggestion (Toggle 2 must be a human-only switch — epic #854 safety property). Pre-tray, the chat could call `update_behavior_target` immediately and the change shipped without a human gate. **When writing a new AI tool that proposes a config change, push into the tray with `aiSuggested: true` — do NOT call the config helper directly.** |
| `lib/learner-scope.ts::resolveCallerScopeForReading` (read path, #977) + `tests/lib/learner-scope.test.ts` (9 vitests) | STUDENT-scope guard for admin-style multi-caller GET routes that accept `?callerId=`. STUDENT sessions are locked to their own LEARNER `Caller` regardless of the supplied query param; OPERATOR+ passes through unchanged. Wired into `app/api/calls/route.ts`, `app/api/goals/route.ts`, `app/api/memories/route.ts`. | A STUDENT session passing a foreign `?callerId=` to read another learner's calls / goals / `CallerMemory` (PII) — confirmed leak class on the three routes pre-#977 because `requireAuth("VIEWER")` admits both STUDENT and VIEWER at role level 1. **When writing a new GET route that accepts a caller-scoped param AND admits STUDENT-level sessions, route the param through `resolveCallerScopeForReading` before the Prisma `where`.** Verified live 2026-05-30 (STUDENT cannot read victim caller's data via foreign param; OPERATOR+ admin browsing preserved). |
| `lib/intake/hf-adapter/disclosure-store.ts::deriveDisclosureId(intentId, requirementId)` + 3-route call-site discipline (#1048) | Single deterministic synthetic-id source for `tallyseal_disclosure` rows. `bootstrap/route.ts` records with `deriveDisclosureId(...)`; `disclosure-acknowledge/route.ts` calls `markAcknowledged(...)` against the same derived id; `disclosure-signal/route.ts` derives the id server-side from `body.intentId + body.signal.requirementId` rather than trusting the client-supplied `signal.disclosureId` (client field is observed but NOT routed to the store call). Q-CR9 structural SIGNAL-not-gate enforcement: `recordSignal()` writes only to `tallyseal_disclosure_signal`; `markAcknowledged()` is the ONLY mutation of `tallyseal_disclosure.acknowledged_at`. Best-effort write — Postgres failure logs but doesn't break intake (Q-BRIDGE-RECORDER-DURABILITY tracks the durability gap). | (a) Two concurrent EnrollmentIntake intents colliding on a shared `disc_<requirementId>` PK if intentId wasn't part of the derivation; (b) a tampered client supplying a foreign `signal.disclosureId` to attribute a signal to someone else's disclosure (server now ignores the client field); (c) DisclosureSignal accidentally mutating `acknowledged_at` (structurally impossible — separate tables + separate store methods). |
| `lib/curriculum/ensure-primary-playbook-link.ts::ensurePrimaryPlaybookLink(tx, { playbookId, curriculumId })` + 3-route call-site discipline (#1202/#1203/#1204, `bb4a4b27`) | Idempotent helper that guarantees a `PlaybookCurriculum(role:'primary')` join row for every Curriculum write. Throws on empty `playbookId` / `curriculumId`. Existing rows (including `linked` variants) are left untouched — promotion to `primary` must be explicit. Wired into `POST /api/curricula`, `POST /api/courses/[courseId]/regenerate-curriculum`, and `POST /api/subjects/[subjectId]/curriculum` (fresh-mint branch), each inside the same `$transaction` as the Curriculum write. **When adding a new Curriculum-writing route, call this helper inside the same transaction — do not hand-roll `playbookCurriculum.create`.** See `docs/CONTRACTS-PLAYBOOK-CURRICULUM.md` §3. | Orphan Curriculum rows (the #1184 / #1192 bug class). Without the canonical primary join row, variant-aware readers (`resolvePlaybookIdForCurriculum`) can't fan out, and #1177 Slice 6 (drop `Curriculum.playbookId`) would silently lose every orphaned Curriculum. Pre-fix the three routes wrote `Curriculum` + the deprecated FK only — recreating the #1184 bug class on every call. |
| `lib/compose/bump-timestamp.ts::bumpPlaybookComposeTimestamp` + `lib/compose/bump-curriculum-fanout.ts::bumpCurriculumComposeFanout` + 4-route call-site discipline (#1268 / closeout of Course Design Console #1263 Slice 3) | Educator-driven writes that change compose inputs MUST advance `Playbook.composeInputsUpdatedAt` so the Preview lens (#1268) can flip its staleness signal. Wired into `POST /api/courses/[courseId]/regenerate-curriculum` (Curriculum mutation → `bumpCurriculumComposeFanout(curriculumId)` to fan out across every linked Playbook), `POST /api/courses/[courseId]/import-modules` (`Playbook.config.modules` + `CurriculumModule` rows → `bumpPlaybookComposeTimestamp(courseId)`), `POST /api/courses/[courseId]/course-reference` (new upload route — `bumpPlaybookComposeTimestamp(courseId)` after `PlaybookSource` link), and `POST /api/playbooks/[playbookId]/subjects` (`bumpPlaybookComposeTimestamp(playbookId)` after a successful `PlaybookSubject` upsert). **When adding a new route that mutates a compose-input source (Curriculum, CurriculumModule, LearningObjective, `Playbook.config.modules`, `PlaybookSubject`, `PlaybookSource(COURSE_REFERENCE)`), call the appropriate bump helper AFTER the mutation succeeds; do NOT call it speculatively or inside the pipeline (the COMPOSE stage runs at end-of-pipeline and a mid-pipeline bump would produce a false "not stale" verdict on the next call — see `bump-timestamp.ts` "When NOT to call these" header).** | The 4-route staleness coverage gap Tech Lead flagged during #1263 Slice 3 — pre-fix, Preview's `?Refresh` button was the only way to invalidate the prompt cache for these workflows. A teacher could regenerate-curriculum / import-modules / upload-course-ref / link-Subject, and the next learner call would silently reuse the stale `ComposedPrompt` because nothing advanced `Playbook.composeInputsUpdatedAt`. |
| `lib/voice/create-call-entering-pipeline.ts::createCallEnteringPipeline({callerId, source, voiceProvider, requestedModuleId?})` + 2-route adoption discipline (#1333) + `eslint-rules/no-bare-call-create.mjs` (allow-list of 11 sites, error severity from day 1) + `scripts/check-fk-consistency.ts` WARN-only detector `voice-call-null-playbook-attribution` | Single chokepoint for pipeline-entry Call creation. Resolves the `(playbookId, requestedModuleId, curriculumModuleId)` triple at create-time via the canonical cascade: `resolveActivePlaybookId(callerId)` → `args.requestedModuleId ?? Caller.lastSelectedModuleId ?? null` → `resolveModuleByLogicalId(curriculumId, slug) ?? resolveDefaultModuleForCaller(callerId)`. Does NOT throw on missing enrollment — returns `{ playbookId: null, requestedModuleId: null, curriculumModuleId: null }` so zero-enrollment harness paths (`sim-runner`, `onboarding-call`, brand-new callers) still write a placeholder. Wired into `POST /api/voice/calls/outbound-dial` (Stage A — the route that drifted) and `POST /api/voice/calls/start` (Stage A — the reference impl, net-zero behavioural change). Stage B (`externalId` stamp after the provider's `POST /call`) stays separate; Stage C (`persistEndOfCall`) stays out of scope per TL revision #1. **When writing a new code path that creates a Call row that will enter the pipeline, route through this builder — do NOT call `prisma.call.create` directly. ESLint `hf-call/no-bare-call-create` blocks the regression.** | The `outbound-dial` vs `start` route drift that produced Bertie Tallstaff's orphan Calls 2 + 3 on hf_sandbox 2026-06-08 (`playbookId = NULL`, `curriculumModuleId = NULL`). Pre-fix the outbound-dial route hand-rolled `prisma.call.create({ data: { callerId, source, voiceProvider, transcript } })` and silently dropped the three FKs the chain-contract Link 3 pre-condition requires; downstream COMPOSE then wrote scopeless `ComposedPrompt` rows that no `(callerId, playbookId)` lookup could find, leaving the sim UI's Call 4 with no usable prompt. The builder + the ESLint rule make the drift structurally impossible; the WARN-only SQL detector keeps the pre-fix population visible as forensic evidence (do NOT backfill — risks setting the wrong playbookId for callers who changed enrollment between calls). |
| `lib/goals/strategies/types.ts::StrategyKey` (`as const` object — `skill_ema` / `lo_rollup` / `assessment_readiness` / `connect_warmth_avg` / `manual_only`) + `eslint-rules/no-bare-strategy-key.mjs` (error severity from day 1) + `tests/lib/mastery-roundtrip.test.ts` (round-trip pin) — **mastery-write canonical contract** (#1599) | Single chokepoint for `Goal.progressStrategy` assignment. Any object literal property named `progressStrategy` whose value is a string Literal not in `{skill_ema, lo_rollup, assessment_readiness, connect_warmth_avg, manual_only}` fails CI. Allow-list: `lib/goals/strategies/registry.ts` (the `STRATEGY_ALIASES` map carries historical keys like `lo_mastery` as map keys) + test files. **Per-LO mastery read/write surface contract:** authoritative DB column is `CallerAttribute` with key shape `curriculum:{specSlug}:lo_mastery:{moduleSlug}:{loRef}` — never `Call.loScoresJson` (arithmetic mean, divergent value), never a bare `lo_mastery:{moduleSlug}:{loRef}` (#611 Fix A canonical slug-form requirement). The only valid `progressStrategy` for LEARN goals tracking per-LO mastery is `StrategyKey.lo_rollup`. **When writing a new surface that reads or writes LO mastery, read this row first; reach for `StrategyKey.<member>` and the `CallerAttribute` column.** Companion: `.claude/rules/verify-before-fix.md` mastery-progress fingerprint. | The four-PR fix chain over 2026-06-13 (#1554 `LO_MASTERY` uppercase seed → silent fallback to `manual_only` → cohort frozen at 0; #1561 `loScoresJson` vs `CallerAttribute` divergent reads — Goal.progress matched neither writer; #1573 `upsertParameters` silently dropping `tierScheme` + `tiers` → Skills Framework lens always 3-tier default; #1552 wizard `create_course` hand-rolled `ContentAssertion` write with 4 wrong field names → outer try/catch logged "non-fatal" and every wizard-created course shipped with zero pedagogy assertions). All four shared the same shape: a new write site hand-rolled a shape next to existing patterns and diverged. The enum + ESLint rule + canonical-column declaration make the divergence structurally impossible at lint time; the round-trip vitest pins the write→read shape so a future refactor that changes one side fails the bank before reaching hf_sandbox. |
| `lib/curriculum/mark-module-incomplete.ts::markModuleIncomplete(tx, {callerId, moduleId, courseStyle, playbookId?, durationSeconds?, minSpeakingSec?})` + `eslint-rules/no-bare-module-progress-update.mjs` (error severity from day 1; allow-list = canonical writers + admin reset routes + backfill scripts + tests) + sticky-waiver guard in `lib/curriculum/track-progress.ts:665` (existing.incompleteAttempts ≥ 2 + status="COMPLETED" → preserve) — **incomplete-attempt canonical contract** (#1703 / epic #1700 Theme 9) | Single chokepoint for `CallerModuleProgress.incompleteAttempts` writes. **Atomic increment via Prisma `{ increment: 1 }`** (Postgres row-level lock — two concurrent endSession webhooks cannot both observe `attempts = 0` and both decide "no waiver"; the second observes the incremented value). **Default-deny on continuous courses** — guard #1252 (`no-module-read-without-course-style-guard`): helper takes `courseStyle: CourseStyle` arg and short-circuits with `{attempts: 0, waived: false}` + AppLog `module.incomplete.skipped_continuous` when not "structured". **Waiver invariant:** when `existing.incompleteAttempts >= 1` (i.e. this is the second-or-later incomplete attempt), the helper sets `status="COMPLETED"` + `completedAt=now` so the picker stops re-prompting. **Sticky-waiver:** `lib/curriculum/track-progress.ts:665` reads `existing.incompleteAttempts + existing.status`; when `>= 2 && COMPLETED`, the canonical mastery writer preserves the status rather than recomputing from `finalMastery`. Without this guard, a low-mastery pipeline run after waiver would clobber back to NOT_STARTED/IN_PROGRESS (the chain-contract race the helper closes). **AppLog subjects:** `module.incomplete.recorded` (first attempt) / `module.incomplete.waived` (second attempt → waiver fires) / `module.incomplete.skipped_continuous` (default-deny) / `module.incomplete.no_progress_row` (missing CallerModuleProgress row — loud-skip). **When writing a new path that exits a session below a per-module completion gate, route through this helper — do NOT call `prisma.callerModuleProgress.update` directly. ESLint `hf-curriculum/no-bare-module-progress-update` blocks the regression.** | Two-writer drift between mastery (`track-progress.ts:665`, status from finalMastery) and incomplete-attempts (this helper, status from waiver policy). Pre-fix, a hand-rolled `incompleteAttempts: 1` write would (a) race with concurrent webhooks (two short-call endings both observe 0), (b) silently get clobbered by the next pipeline run (track-progress overwrites status from low finalMastery), (c) leak through the continuous-course gate (a topic-pool conversation should not have a "second-attempt waiver" concept). The chokepoint helper + ESLint rule + sticky-waiver collaboration close all three simultaneously. Migration B (`CallerModuleProgress.incompleteAttempts INT NOT NULL DEFAULT 0`) provides the column. |
| `lib/voice/create-session.ts::createSession({callerId, kind, source?, voiceProvider?, requestedModuleId?, intentId?})` + `lib/voice/end-session.ts::endSession(sessionId, {outcome, transcript?, endSource?, …})` + 5-route adoption discipline (#1342) + `eslint-rules/no-bare-call-create.mjs` extended to also block `prisma.session.create` outside allow-list + `HF_FLAG_SESSION_MODEL_V2` feature flag for staged rollout | Canonical builders for the unified Session model from epic #1338. `createSession` atomically assigns `sequenceNumber` via `CallerSequenceCounter.upsert({update: {nextSeq: {increment: 1}}})` (Postgres row-level lock serialises concurrent webhooks — race-safe), snapshots `resolveVoiceConfig()` into `voiceConfigSnapshot` (forensics + reproducibility), walks the I-CT2 cascade for `usedPromptId` (previous Session → most-recent ACTIVE ComposedPrompt → ENROLLMENT bootstrap), and derives `skipStages` + `countsTowardLearnerNumber` from `kind`. `endSession` commits `endedAt` + `status` + retroactively flips counter flags false when duration falls below `DEFAULT_MIN_LEARNER_DURATION_SECONDS` (30s) or outcome is GHOST/FAILED; fires `runSpecDrivenPipeline` fire-and-forget (NEVER awaited, NEVER inside the transaction — wrapping the LLM call in a tx means a 5s timeout destroys the transcript). Wired into `POST /api/callers/[callerId]/calls` (sim), `POST /api/test-harness/onboarding-call`, `POST /api/join/[token]` (ENROLLMENT via `maybeWriteEnrollmentSession` helper, both new + returning paths), `lib/test-harness/sim-runner.ts` (harness sim), and `lib/voice/route-handlers.ts::persistEndOfCall` (both adopted-placeholder merge AND fresh-arrival create paths). The legacy `createCallEnteringPipeline` becomes a thin wrapper delegating to `createSession({kind: VOICE_CALL, ...})` when the flag is on — preserves its existing call sites (`/api/voice/calls/outbound-dial`, `/api/voice/calls/start`) without modification. **When writing a new code path that records a learner interaction (VOICE_CALL / SIM_CALL / TEXT_CHAT / ENROLLMENT / ASSESSMENT), route through `createSession` + `endSession` — do NOT call `prisma.session.create` directly. ESLint `hf-call/no-bare-call-create` blocks the regression.** See `docs/CHAIN-CONTRACTS.md` §3 Link 3b for the Session-boundary invariants. | The `MAX(callSequence)+1` race in `route-handlers.ts:577-589` that produced Bertie Tallstaff's duplicate-sequence ghosts on hf_sandbox 2026-06-08 — two concurrent VAPI webhook deliveries within ms read the same MAX value and wrote two rows with the same `callSequence`. Atomic `UPDATE ... RETURNING` against `CallerSequenceCounter` is the canonical replacement (Postgres row-level lock). Also blocks: (a) drift in the `usedPromptId` resolution cascade — pre-#1342 each route hand-rolled its own fallback, with some skipping the bootstrap step; (b) `voiceConfigSnapshot` ever going un-stamped (live config drift hides what was active at session-start); (c) sim drops inflating the learner-facing `(call #N)` counter (the SIM_CALL class doesn't `countsTowardLearnerNumber`); (d) FAILED/GHOST sessions running EXTRACT/SCORE_AGENT/PROSODY/REWARD against an empty transcript (pipeline runner reads `skipStages`). Flag-gated via `HF_FLAG_SESSION_MODEL_V2=true` for staged rollout — default off; on flips routes through builders and starts writing Session rows + Call.sessionId links. Slice 4 enforces NOT NULL on `Call.sessionId` after the verification window. |

## Known Gaps (tech debt)

- `structure-assertions.ts`: deleteMany + rebuild without transaction — AI tree error = data loss
- `extract-curriculum.ts`: no max-module-count enforcement in code (only in prompt)
- Pipeline `parameterId`: FK constraint is only guard — no pre-filter against known params
- `callerMemory.create`: no count limit per call, no key/value length cap

## Escalation

If you're writing a new AI-to-DB path and can't add a structural guard, add a `// TODO(ai-guard):` comment explaining why and what the risk is. These are tracked by `broken-windows` agent.
