# Writer Coverage

> Generated by `apps/admin/scripts/generate-writer-coverage-doc.ts` from `apps/admin/lib/contracts/writer-registry.ts`. Do not edit by hand — re-run the generator after registry changes.

Pairs every nullable Prisma field that has a UI/API reader with the writer function + pipeline stage + invocation trigger that's expected to populate it. The arch-checker integration (`check-writer-registry.ts`) validates these pairings at PR time so a reader added without a registered writer fails CI before reaching production.

**Maintenance contract:** see the JSDoc on `WRITER_REGISTRY` in `writer-registry.ts`.

## Coverage by stage

### ADAPT

| Field | Writer | Reader | Trigger | Closed by |
|---|---|---|---|---|
| `RewardScore.targetUpdatesApplied` | `lib/ops/update-targets.ts::updateTargets` | `app/api/callers/[callerId]/adaptations/route.ts` | per-call | #1609 / PR #1629 |
| `Goal.progressMetrics` | `lib/goals/append-progress-entry.ts::appendGoalProgressEntry (invoked from lib/goals/track-progress.ts::trackGoalProgress)` | `app/api/callers/[callerId]/attainment/route.ts::buildGoalTrail` | per-call | #1614 / PR #1627 |

- **RewardScore.targetUpdatesApplied** — Per-call reward-feedback loop closure: BehaviorTarget adjustments applied, or `[]` sentinel when within tolerance.
- **Goal.progressMetrics** — Per-call evidence trail + mentionCount + lastMentionedAt for each goal (SP4-D evidence trail).

### REWARD

| Field | Writer | Reader | Trigger | Closed by |
|---|---|---|---|---|
| `RewardScore.effectiveTargets` | `app/api/calls/[callId]/pipeline/route.ts::computeReward` | `lib/ops/update-targets.ts::updateTargets` | per-call | #1641 / PR #1648 |

- **RewardScore.effectiveTargets** — Snapshot of the SYSTEM+PLAYBOOK cascade-merged targets at REWARD time. Required by updateTargets to compute adjustments downstream.

### SCORE_AGENT

| Field | Writer | Reader | Trigger | Closed by |
|---|---|---|---|---|
| `BehaviorMeasurement.evidence` | `app/api/calls/[callId]/pipeline/route.ts::buildBatchedAgentPrompt + lib/pipeline/normalize-score-agent-evidence.ts::normalizeScoreAgentEvidence` | `components/callers/caller-detail/AttainmentTab.tsx::SkillEvidencePanel` | per-call | #1608 / PR #1613 |

- **BehaviorMeasurement.evidence** — Verbatim learner quotes from transcript supporting each behavior score (SP4-A evidence trail).

## Full row dump

```json
[
  {
    "field": "BehaviorMeasurement.evidence",
    "writer": "app/api/calls/[callId]/pipeline/route.ts::buildBatchedAgentPrompt + lib/pipeline/normalize-score-agent-evidence.ts::normalizeScoreAgentEvidence",
    "stage": "SCORE_AGENT",
    "expectedTrigger": "per-call",
    "reader": "components/callers/caller-detail/AttainmentTab.tsx::SkillEvidencePanel",
    "description": "Verbatim learner quotes from transcript supporting each behavior score (SP4-A evidence trail).",
    "closedBy": "#1608 / PR #1613"
  },
  {
    "field": "RewardScore.targetUpdatesApplied",
    "writer": "lib/ops/update-targets.ts::updateTargets",
    "stage": "ADAPT",
    "expectedTrigger": "per-call",
    "reader": "app/api/callers/[callerId]/adaptations/route.ts",
    "description": "Per-call reward-feedback loop closure: BehaviorTarget adjustments applied, or `[]` sentinel when within tolerance.",
    "closedBy": "#1609 / PR #1629"
  },
  {
    "field": "Goal.progressMetrics",
    "writer": "lib/goals/append-progress-entry.ts::appendGoalProgressEntry (invoked from lib/goals/track-progress.ts::trackGoalProgress)",
    "stage": "ADAPT",
    "expectedTrigger": "per-call",
    "reader": "app/api/callers/[callerId]/attainment/route.ts::buildGoalTrail",
    "description": "Per-call evidence trail + mentionCount + lastMentionedAt for each goal (SP4-D evidence trail).",
    "closedBy": "#1614 / PR #1627"
  },
  {
    "field": "RewardScore.effectiveTargets",
    "writer": "app/api/calls/[callId]/pipeline/route.ts::computeReward",
    "stage": "REWARD",
    "expectedTrigger": "per-call",
    "reader": "lib/ops/update-targets.ts::updateTargets",
    "description": "Snapshot of the SYSTEM+PLAYBOOK cascade-merged targets at REWARD time. Required by updateTargets to compute adjustments downstream.",
    "closedBy": "#1641 / PR #1648"
  }
]
```

